using System;

namespace Compilers
{
    /**
     * Enumeration VALTYPE : it has the types that
     * a variable can have (int, string or bool)
     */
    public enum VALTYPE
    {
        INT,
        STRING,
        BOOL
    }
}
